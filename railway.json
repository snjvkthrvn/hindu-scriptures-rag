{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir /app/english-v1-rag --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - app:app",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
