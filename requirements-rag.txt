# RAG system dependencies — Qdrant + Cohere + Claude stack
cohere>=5.0
qdrant-client>=1.7.0
fastembed>=0.2.0
anthropic>=0.40.0
tqdm>=4.64.0
flask>=3.0.0

# Optional: Ollama fallback (uncomment if needed)
# langchain>=0.3.0
# langchain-core>=0.3.0
# langchain-ollama>=0.2.0
